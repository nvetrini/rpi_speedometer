common:
  tags:
    - unit
    - application
    - wheel_sensor
    - sensor
  depends_on: ztest

tests:
  app.wheel_sensor:
    tags:
      - wheel_sensor
      - sensor
    min_ram: 8
    timeout: 30
    filter: test_wheel_sensor