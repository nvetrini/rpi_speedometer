common:
  tags:
    - unit
    - application
    - sd_card
    - filesystem
    - fs
  platform_allow:
    - native_sim

tests:
  app.sd_card:
    tags:
      - sd_card
      - filesystem
      - fs
    min_ram: 8
    timeout: 30
    extra_args: EXTRA_DTC_OVERLAY_FILE=boards/native_sim.overlay
